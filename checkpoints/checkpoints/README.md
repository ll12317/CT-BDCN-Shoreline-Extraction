# Model checkpoints

The trained model weights are not included in this repository because of file size limitations.

Users can reproduce the model weights by running the training scripts provided in the `scripts/` folder.

If required, trained weights can be provided by the corresponding author upon reasonable request.